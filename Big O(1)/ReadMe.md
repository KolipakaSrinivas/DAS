1. What is good cod
    # Readble
    # Scalable

2. for(const value of array) {
        if(value === 'nemo') {
            console.log('found nemo')
        }
    } 
    ##  0(n) ==> Linear Time

3. function compressFirstBox(box) {
     console.log(box[0])
    }
    # 0(1) ==> Constant Time
    # No matter console.log(box[0]),console.log(box[0]),console.log(box[0])

4. scalable => Readble and Memorey and Time

5. 